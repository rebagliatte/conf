<header class="admin-header">
  <%= title @talk.title %>
  <%= link_to 'Edit', edit_admin_conference_edition_talk_path(@conference_edition, @talk), class: 'btn btn-primary main-cta' %>
</header>

<%= render(partial: 'admin/shared/locale_tabs', locals: { translations: @talk.translations }) %>

<table class="table translated-table">
  <tr>
    <th>ID</th>
    <td><%= @talk.id %></td>
  </tr>
  <tr>
    <th>Title</th>
    <td><%= @talk.title %></td>
  </tr>
</table>
