<h1 class="primary-heading">Conferences</h1>

<ul>
  <% @conferences.each do |conference| %>
    <li><%= link_to conference.name, root_url(:subdomain => conference.subdomain) %></li>
  <% end %>
</ul>

<%= link_to 'Create a new conference', new_admin_conference_path, class: 'btn btn-primary' %>
