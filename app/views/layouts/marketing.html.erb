<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title>Confnu</title>
  <meta name="description" content="Conference organization made simple">
  <meta name="viewport" content="width=device-width">
  <meta charset="utf-8">
  <%= stylesheet_link_tag 'marketing', media: 'all' %>
  <%= csrf_meta_tags %>
  <%= render partial: 'shared/analytics' %>
</head>

<body>
  <header class="main-header">
    <h1><%= image_tag('marketing/confnu.jpg', alt: 'Confnu') %></h1>
  </header>

  <main class="main-content">
    <% flash.each do |name, msg| %>
      <p class="alert alert-<%= name %>"><%= msg %></p>
    <% end %>

    <%= content_for?(:content) ? yield(:content) : yield %>
  </main>

  <!--<footer class="main-footer">
    <%= link_to('Create an event', '#', class: 'create-an-event') %>
    <ul>
      <li><%= link_to('About', '#', class: 'about') %></li>
      <li><%= link_to('Contact', '#', class: 'contact') %></li>
    </ul>
  </footer> -->
  <%= javascript_include_tag 'application' %>
</body>
</html>
