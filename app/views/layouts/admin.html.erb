<!DOCTYPE html>
<html lang="en">
<head>
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title><%= yield(:title) %> - Confnu Admin </title>
  <meta name="description" content="Confnu Admin">
  <meta name="viewport" content="width=device-width">
  <meta charset="utf-8">
  <link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" rel="stylesheet">
  <%= stylesheet_link_tag 'admin', media: 'all' %>
  <%= csrf_meta_tags %>
  <%= render partial: 'shared/analytics' %>
</head>

<body class="admin">
  <header class="navbar main-admin-header">
    <div class="container">
      <%= link_to 'Confnu ADMIN', admin_conferences_path, class: 'brand' %>
      <%= render(partial: 'shared/authentication') %>
    </div>
  </header>

  <div class="row-fluid">
    <% if current_user %>
      <nav class="main-admin-sidebar span2">
        <ul>
          <% conferences.each do |conference| %>
            <li>
              <%= link_to conference.name, admin_conference_path(conference), class: 'conference-nav-item' %>

              <ul>
                <% conference.conference_editions.each do |e| %>
                  <li>
                    <%= link_to pretty_year(e), admin_conference_conference_edition_path(conference, e), class: 'conference-edition-nav-item' %>
                    <ul>
                      <li><%= link_to 'Talks', admin_conference_edition_talks_path(e) %></li>
                      <li><%= link_to 'Speakers', admin_conference_edition_speakers_path(e) %></li>
                      <li><%= link_to 'Schedule', admin_conference_edition_slots_path(e) %></li>
                      <li><%= link_to 'Subscribers', admin_conference_edition_subscribers_path(e) %></li>
                      <li><%= link_to 'Notifications', admin_conference_edition_notifications_path(e) %></li>
                      <li><%= link_to 'Pages', admin_conference_edition_pages_path(e) %></li>
                      <li><%= link_to 'Posts', admin_conference_edition_posts_path(e) %></li>
                      <li><%= link_to 'Organizers', admin_conference_edition_organizers_path(e) %></li>
                      <li><%= link_to 'Sponsors', admin_conference_edition_sponsors_path(e) %></li>
                      <li><%= link_to 'Appearance', appearance_admin_conference_conference_edition_path(conference, e) %></li>
                    </ul>
                  </li>
                <% end %>
              </ul>
            </li>
          <% end %>
        </ul>
      </nav>
    <% end %>

    <main class="main-admin-content-area <%= current_user ? 'span10' : 'span12' %>">
      <% flash.each do |name, msg| %>
        <p class="alert alert-<%= name %>"><%= msg %></p>
      <% end %>

      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </div>

  <footer class="admin-footer">
  </footer>

  <%= javascript_include_tag 'application' %>
</body>
</html>
