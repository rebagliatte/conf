<% if object && object.errors.size > 0 %>
  <div class="alert alert-error">
    <p><strong><%= t 'oh_snap' %> </strong><%= t 'please_resubmit' %></p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
